# 项目路线图

## 里程碑 0：逆向与协议破解 ✅（已完成，2026-09-06）

- [x] 解包 Inno Setup 6.3.0 安装包（绕过官方工具不支持，自写 LZMA 解析 + PE 雕刻，还原 371 个文件）
- [x] 反编译主程序集 `蛟龙游戏控制中心.dll`（ILSpy → 5150 行 C#）
- [x] 破解 WMI 二进制协议（32 字节透传，命令表 8–23）
- [x] 本机只读实测 16 条命令全部通过（风扇/温度数据与实际硬件状态吻合）
- [x] 建立术语表、协议手册、Git 仓库

## 里程碑 1：命令行工具 jiaolongctl（当前）

目标：把已验证的协议变成可用的 Python 工具。

- [ ] `wmi_interface.py` — WMI 封装（pywin32 或 PowerShell 桥）
- [ ] `status` / `get` / `set` 三个子命令
- [ ] 写操作加 `--write` 显式确认（安全设计）
- [ ] 实测写操作：先测 `RGBKeyboardStatus`（风险最低：键盘灯开关）
- [ ] 打包为 pip 可安装包

## 里程碑 2：完整功能覆盖

- [ ] 风扇曲线读取/设置（扩展命令码扫描，找 RPM 写入口）
- [ ] SPL / SPPT / 温度墙的实测写验证
- [ ] 键盘 RGB（模式/颜色/亮度）写验证
- [ ] GPU 超频：放弃 KaronOC.dll（闭源），改用 NVAPI 直接实现
- [ ] 热键事件订阅（HID_EVENT20）→ 可做 OSD 提示

## 里程碑 3：图形界面

- [ ] 技术选型：建议 **Tauri**（Rust + Web 前端，体积小）或 **Avalonia**（.NET，与 Windows 生态一致）
- [ ] 你已会 React/TSX → Tauri 前端可以复用现有技能
- [ ] 功能：仪表盘（温度/风扇/功耗实时曲线）、模式切换、RGB 调色板、功耗滑条

## 里程碑 4：开源发布

- [ ] 多机型支持设计（协议做成 profile 表，因为 SSDT 随 BIOS 变化）
- [ ] 发布到 GitHub（先确认不含厂商二进制：`git ls-files` 检查）
- [ ] 编写贡献指南：新机型适配流程（跑 probe 脚本 → 填 profile）
- [ ] CI：GitHub Actions 跑静态检查

## 风险清单

| 风险 | 缓解 |
|---|---|
| 写错功耗值导致死机/降频保护 | 写操作前读回当前值，异常值拒绝写入（对齐反编译里的范围检查） |
| BIOS 更新后协议变化 | probe 脚本可重复运行做回归测试；profile 表按 BIOS 版本组织 |
| GPU 模式切换写坏启动 | 独显/混合切换写命令**最后实现**，写前弹窗强警告（需要重启才生效） |
| 厂商二进制误入库 | .gitignore 已排除；发布前 `git ls-files | grep -iE '\.(dll|exe)$'` 复查 |
